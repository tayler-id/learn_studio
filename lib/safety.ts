export function safeImagePrompt(base: string) {
  return `${base}. Kid-safe, positive, no brands, high contrast, clear shapes, large labels, friendly style.`;
}

export function safeVideoScript(base: string) {
  return `${base} Use friendly tone; avoid fear/violence; captions on.`;
}
